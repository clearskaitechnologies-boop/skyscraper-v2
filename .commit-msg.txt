fix: critical bug sweep + feature deletion + claims UI enhancements

Bug Fixes:
- Fix client portal dashboard (remove /portal from public routes)
- Fix pipeline lifecycle_stage ACTIVE crash (Zod + mapping alignment)
- Fix leaderboard showing demo/test data (isDemo:false filter)
- Fix trades profile widget for existing users (broaden self-heal)
- Fix company page no-company error (lookup existing before create)
- Fix weather analytics HTTP 402 (isBetaMode bypass in billing)

Feature Deletions:
- Remove Community feature entirely (19 files)
- Remove Batch Reports feature entirely
- Clean sidebar, nav, reports hub, sectionThemes

New Features:
- Claims sidebar (value, dates, adjuster, quick actions, property)
- Claims overview grid layout with sidebar
- Manager/downline hierarchy (schema, migration, API, UI)
- Job value pill in claims header
- Zod domainSchemas aligned to Prisma enum

0 TypeScript errors
