"use client";
import React from "react";

export default function ColorsTypographyTab({ orgId }: { orgId?: string }) {
  return (
    <div>
      <h2 className="text-lg font-semibold">Colors & Typography</h2>
      <div className="mt-4 text-sm text-gray-600">
        <p>Branding customization is coming soon with the subscription system.</p>
        <p>This feature will be available for Business and Enterprise plans.</p>
      </div>
    </div>
  );
}
