/**
 * Get claim assets with metadata
 * Stub: fileAsset model removed from schema. Returns empty list.
 */
export async function getClaimAssetsWithMetadata(claimId: string) {
  console.log(`[getClaimAssetsWithMetadata] Stub for claim ${claimId} — fileAsset model removed`);
  return { assets: [] };
}
